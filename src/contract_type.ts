// import { nativeToScVal } from 'stellar-sdk';

// export abstract class ContractType<T> {
//   abstract latestLedger: number;
//   abstract fromXDR(xdr: string): T;

//   toXDR(): string {
//     return nativeToScVal(this, T).toXDR('base64');
//   }
// }
